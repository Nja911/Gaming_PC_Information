import Link from "next/link";
import type { Comparison, ComparisonSide } from "@/types";
export function ComparisonCard({ comparison }: { comparison: Comparison }) { return <Link href={`/comparisons/${comparison.slug}`} className="group block border-t border-line py-6"><div className="flex gap-5"><span className="text-sm text-dim">Compare</span><div><p className="mb-2 text-sm text-dim">{comparison.left.name} <span className="text-accent">vs</span> {comparison.right.name}</p><h2 className="font-display text-2xl group-hover:text-accent sm:text-3xl">{comparison.title}</h2></div></div></Link>; }
function SideColumn({ side }: { side: ComparisonSide }) { return <div className="border-t border-line py-6"><h3 className="font-display text-3xl mb-5">{side.name}</h3><dl className="space-y-3 text-sm"><Row label="Architecture" value={side.architecture} />{side.vramOrCache && <Row label="VRAM / cache" value={side.vramOrCache} />}<Row label="Performance category" value={side.performanceCategory} /><Row label="Power consumption" value={side.powerConsumption} /><Row label="Target resolution" value={side.targetResolution} /></dl><div className="mt-6"><h4 className="mb-2 text-xs uppercase tracking-[.15em] text-accent">Strengths</h4><ul className="space-y-1 text-sm text-dim">{side.strengths.map((s) => <li key={s}>+ {s}</li>)}</ul></div><div className="mt-5"><h4 className="mb-2 text-xs uppercase tracking-[.15em] text-dim">Weaknesses</h4><ul className="space-y-1 text-sm text-dim">{side.weaknesses.map((w) => <li key={w}>− {w}</li>)}</ul></div><p className="mt-5 border-t border-line-soft pt-4 text-sm"><span className="text-dim">Best for: </span>{side.recommendedFor}</p></div>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-4 border-b border-line-soft pb-2"><dt className="text-dim">{label}</dt><dd className="text-right">{value}</dd></div>; }
export default function ComparisonLayout({ comparison }: { comparison: Comparison }) { return <div className="grid gap-8 md:grid-cols-2"><SideColumn side={comparison.left} /><SideColumn side={comparison.right} /></div>; }
